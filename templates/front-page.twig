{% extends "base.twig" %}

{% block content %}

    {% include 'partial/slider.twig' %}

    <div class="content-wrapper" style="background-color: pink; height: 500px;">
        {#<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
            <section class="article-content">
                <h1 class="article-h1">{{post.title}} ssdds</h1>
                <div class="article-body">
                    {{post.content}}
                </div>
            </section>
        </article>#}
    </div>

{% endblock %}